Index: triton/python/setup.py
===================================================================
--- triton.orig/python/setup.py	2024-04-04 15:07:18.857522102 -0500
+++ triton/python/setup.py	2024-04-04 16:19:53.739942752 -0500
@@ -237,7 +237,7 @@
             os.getenv("HOMEPATH") or None
         if not user_home:
             raise RuntimeError("Could not find user home directory")
-        triton_cache_path = os.path.join(user_home, ".triton")
+        triton_cache_path = os.path.join(os.getenv("SRC_DIR"), ".triton")
         # lit is used by the test suite
         thirdparty_cmake_args = get_thirdparty_packages(triton_cache_path)
         extdir = os.path.abspath(os.path.dirname(self.get_ext_fullpath(ext.path)))
